{
  "name": "gabaritei-os",
  "version": "1.0.0",
  "private": true,
  "description": "Testes de domínio, integração e segurança do Gabaritei OS.",
  "scripts": {
    "test": "node --test",
    "test:rules": "firebase emulators:exec --only firestore \"node --test tests/firestore-rules.emulator.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "firebase": "^12.16.0",
    "firebase-tools": "^15.24.0"
  }
}
